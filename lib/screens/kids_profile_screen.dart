import 'package:flutter/material.dart';

class KidsProfileScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Kids Profile'),
      ),
      body: Center(
        child: Text('Kids Profile Screen'),
      ),
    );
  }
}
